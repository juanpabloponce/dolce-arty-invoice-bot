[phases.setup]
nixPkgs = ['nodejs_20', 'python310', 'python310Packages.pip']

[phases.install]
cmds = [
  'npm install',
  'pip3 install --break-system-packages reportlab'
]

[start]
cmd = 'node bot.js'
